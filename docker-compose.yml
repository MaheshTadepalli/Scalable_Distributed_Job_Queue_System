services:
  job-queue:
    build: .
    ports:
      - "8080:8080"
    environment:
      JOB_QUEUE_PORT: 8080
      JOB_QUEUE_WORKERS: 4
      JOB_QUEUE_MAX_RETRIES: 3
      JOB_QUEUE_RETRY_DELAY_MS: 1500
    restart: unless-stopped
